# Data

This folder holds source registers, indicator catalogs, raw inputs, processed outputs, and scenario assumptions.

Do not store secrets here. Do not add large raw downloads directly unless they are intentionally versioned and small enough for Git.

Canonical files:

- `data/sources/source_register.csv`
- `data/indicators/indicator_catalog.csv`

Processed files are placeholders until the Deep Research reports are reviewed.

