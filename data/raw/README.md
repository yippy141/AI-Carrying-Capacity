# Raw Data

Store small, reviewable raw inputs here when they should be versioned.

Large downloads should go in `data/raw/downloads/`, which is ignored by Git.

Every raw data file should have a matching source entry in `data/sources/source_register.csv`.

