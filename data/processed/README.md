# Processed Data

Processed outputs should be generated from reviewed sources and scripts.

Current JSON files are placeholders only. They should not be interpreted as evidence or product data.

`v0_visual_system.json` is staged visual metadata for the frontend. It may include
qualitative watch states and source IDs, but it does not contain reviewed
indicator values or national scores.
