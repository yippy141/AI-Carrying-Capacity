# Components

Reusable UI components for the AI Conversion Atlas web product.

Current organization:

- `layout/`
- `ui/`
- `visuals/`

Do not use components to hide missingness or turn placeholder data into apparent scores.
