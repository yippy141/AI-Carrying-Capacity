# Rendered reader revision — draft PR #43

Captured by Chromium from the local optimized review build, 6 September 2026.
These are real browser captures, not mockups or outside-reader feedback.

Two viewports: desktop 1440×1000, mobile 390×844. Element captures retain the
rendered element's full height. Mobile uses reduced-motion preference.

For each viewport:
- `desktop.png` / `mobile.png`: first screen.
- `*-study.png`: complete continuous study.
- `*-evidence.png`: ECB source/caveat reached from the empirical figure.
- `*-outcomes.png`: distinct normalized operational comparisons.
- `*-actors.png`: verified support-case actor/evidence map.
- `*-mechanism.png`: default independent duration/capacity examples.
- `*-added-work.png`: eight extra days; project 40→42 days (5% more), output 4→4/week.
- `*-fusion.png`: original TCV loop and separate plant dependencies.

`../reader-print.pdf` is the same `/paper` content, printed as 16 A4 pages.
The ZIP contains the sixteen PNGs and this README. It is publicly accessible
through GitHub even though the existing hosted preview requests sign-in.
